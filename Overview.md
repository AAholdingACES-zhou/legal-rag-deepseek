# ⚖️ Chinese Legal RAG System — 基于 DeepSeek + LlamaIndex 的劳动合同法智能问答+期末考复习系统

本项目使用 **DeepSeek Chat + LlamaIndex + BGE 向量模型** 构建一个“可本地运行”的**劳动法律检索增强生成（RAG）问答系统**，目前已支持对《劳动合同法》条文进行检索、引用与基础分析，并加入劳动法案例库。案例库材料来自复旦大学法学院劳动法课程，承担期末复习bot的功能。
![Python](https://img.shields.io/badge/Python-3.11-blue?logo=python)
![DeepSeek](https://img.shields.io/badge/Model-DeepSeekChat-red?logo=openai)
![LlamaIndex](https://img.shields.io/badge/RAG-LlamaIndex-green)
![Embedding](https://img.shields.io/badge/Embedding-BGE_small_zh_v1.5-yellow)
![License](https://img.shields.io/badge/License-MIT-lightgrey)

项目链接：https://github.com/AAholdingACES-zhou/legal-rag-deepseek
---

## 📌 项目亮点（Overview）

- 利用 **DeepSeek Chat** 作为大语言模型，避免直接依赖 OpenAI Key（通俗来说，适用于 OpenAI Key 不方便使用的场景）
- 使用 **BAAI/bge-small-zh-v1.5** 作为中文嵌入模型，构建法律文本向量索引
- 基于 **LlamaIndex** 搭建完整 RAG 流程：本地文档加载 → 向量化 → 索引 → 检索 → LLM 生成
- 支持在本地命令行调用：例如「试用期内能否随意辞退劳动者？」并返回公整回答：结论 + 引用法条【法条号+内容】 + 引用类案
- 自定义适配方式让 DeepSeek Chat 兼容 LlamaIndex 的接口

---

## 🧱 项目结构

> 这是本地项目的大致结构（示例）：

```bash
law_rag_project/
│
├── data/                                    # 本地知识库（Legal Corpus）
│   ├── statutes/                            # 法条库（结构化知识）
│   │   ├── labor_contract_law_1_98.txt      # 《劳动合同法》1–98条（清洗版）
│   │   └── ...                              # （未来可扩：工伤条例/仲裁法等）
│   │
│   ├── cases/                               # 劳动法案例库（类案检索）
│       ├── cases_labor.txt                  # 已清洗的14个类案（Case 1–14）
│       └── ...                              # （未来可扩：民法典雇佣、竞业限制案例等）  
│
├── rag_law_bot.py                           # RAG 主程序（DeepSeek + LlamaIndex）
│                                            # - 支持多索引（法条+案例）
│                                            # - 三段式输出：结论/法条/类案
│ 
├── .env                                     # API Key
│ 
├── Overview.md                              # 项目说明文档（功能介绍 / 设计思路）
│
├── Debug_log.md                             # Debug 全纪录（从0到1完整日志）
│ 
├── RAG_Dev_log.md                           # 调试bot的过程记录
│
└── requirements.txt                         # 最小依赖（llama-index, embedding, dotenv 等）
```

---
## 📌 输出结果展示
```
PS C:\Users\99719> cd C:\law_rag_project
PS C:\law_rag_project> .\.venv311\Scripts\activate
(.venv311) PS C:\law_rag_project> python rag_law_bot.py
已为 OpenAI SDK 设置 base_url = https://api.deepseek.com
✅ 已读取到 OPENAI_API_KEY，准备初始化 LLM 与向量模型...
🔧 已为 LlamaIndex 打补丁，使其支持 deepseek-chat 模型。
📦 正在加载向量模型: BAAI/bge-small-zh-v1.5 ...
🤖 已配置 deepseek-chat 作为对话模型。
📚 正在加载法条文档 ./data/statutes ...
已加载法条文档数量: 1
📚 正在加载案例文档 ./data/cases ...
已加载案例文档数量: 1
🧠 向量索引构建完成！可以开始提问了～

💬 请输入你的问题（输入 q 退出）：
> 用人单位在试用期能不能随意辞退劳动者？

====== 模型回答 ======

1. 结论与分析：
- 用人单位在试用期不能随意辞退劳动者。试用期解除劳动合同必须符合法定情形，且用人单位负有说明理由的义务，否则可能构成违法解除。
- 根据《劳动合同法》第21条，用人单位在试用期解除劳动合同仅限于劳动者存在第39条规定的过失性情形（如被证明不符合录用条件、严重违反规章制度等），或第40条第（一）、（二）项规定的无过失性情形（如患病医疗期满不能从事原工作，或不能胜任工作经调岗或培训后仍不能胜任）。对劳动者有利的是，法律严格限制了试用期解除的条件，且要求用人单位承担举证责任；不利的是，若劳动者确实存在上述法定情形，用人单位可依法解除。需注意，用人单位在解除时必须履行说明理由的程序义务，且不得违反第42条关于禁止解除的特殊规定（如女职工在"三 期"内）。

2. 适用法条及条文内容：
（1）《劳动合同法》第21条【试用期内解除劳动合同】：在试用期中，除劳动者有本法第三十 九条和第四十条第一项、第二项规定的情形外，用人单位不得解除劳动合同。用人单位在试用期解除劳动合同的，应当向劳动者说明理由。
（2）《劳动合同法》第39条【过失性辞退】：劳动者有下列情形之一的，用人单位可以解除劳 动合同：（一）在试用期间被证明不符合录用条件的；（二）严重违反用人单位的规章制度；（三）严重失职，营私舞弊，给用人单位造成重大损害；（四）劳动者同时与其他用人单位建立劳动关系，对完成本单位工作任务造成严重影响，或者经用人单位提出，拒不改正的；（五）因欺诈、胁迫等致使劳动合同无效的；（六）被依法追究刑事责任的。

3. 类案参考：
类案材料中，李某与公司签订三年期劳动合同，约定试用期至2013年1月28日。公司擅自延长试 用期至2月20日，并在试用期届满后以"试用期不合格"为由解除劳动合同。法院认定：第一，公 司单方延长试用期违反《劳动合同法》第19条关于"同一用人单位与同一劳动者只能约定一次试 用期"的规定；第二，试用期届满后，劳动者自动转为正式员工，此时再以"试用期不合格"为由 解除属于违法解除。最终判决公司支付工资差额和违法解除赔偿金。
本案启示：① 试用期期限和次数必须严格依法约定，用人单位无权单方延长；② 试用期届满后解除劳动合同不得再援引试用期特有的解除理由；③ 用人单位在试用期解除必须及时、合法地 履行程序并保留证据。

💬 请输入你的问题（输入 q 退出）：
>
```
<img width="1332" height="980" alt="image" src="https://github.com/user-attachments/assets/a674e174-8ea6-490c-8466-ef0e41c5d1e0" />


